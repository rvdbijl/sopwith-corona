; Compatibility include for the GPL DOS Sopwith source.
;
; The released source references mixed.inc from each assembly module, but the
; copy used for this Corona port does not require any symbols from it.  The
; stack argument offset used by the assembly files is defined in SEGMENTS.H.
